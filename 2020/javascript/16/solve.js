var start = Date.now(); //start Time measurement
//---load puzzle
fs = require('fs');
var input = fs.readFileSync('inputAlFe.txt', 'utf8');


//define rules?

// eliminate invalid ticket an check rate of errors





//check what is what


var timeEnd = Date.now(); //end Time measurement
var runtime = timeEnd -start;  //evaluate Time measurement



//notes
// erster Wert von allen prüfen und einer Art zuweisen, dann kann diese Art aus der Prüfung fallen
// dann zweiter Wert etc.
// zum Schluss eigenens Ticket den Werten zuweisen und solve berechnen